#include<stdio.h>
int main()
{
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    printf("The number is %d\n", n);
    int count=0;
    while (n>0)
    {
        count=count*10+(n%10);
        n=n/10;
    }
    printf("Reverse is %d", count);
    
    return 0;
}
